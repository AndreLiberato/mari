import './bootstrap';
import { Input, initTE } from "tw-elements";
initTE({ Input });
import {
    Collapse,
    Dropdown,
    initTE,
  } from "tw-elements";
  
  initTE({ Collapse, Dropdown });
import {
    Sidenav,
    initTE,
} from "tw-elements";  
initTE({ Sidenav });
import {
    Ripple,
    initTE,
  } from "tw-elements";
  
  initTE({ Ripple });